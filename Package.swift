// swift-tools-version:5.0
import PackageDescription

let package = Package(
    name: "WolfCore",
    products: [
        .library(
            name: "WolfCore",
            targets: ["WolfCore"]),
        ],
    dependencies: [
    ],
    targets: [
        .target(
            name: "WolfCore",
            dependencies: [])
        ]
)
